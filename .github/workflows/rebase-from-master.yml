name: Rebase from Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} from master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-from-master.yml@4c8dfdfc47592da8c6f7d5e7ff795a9007217bac
    permissions:
      contents: write
    secrets: inherit
