name: Rebase from Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} from master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-from-master.yml@6bfd75231afaebd9e9681fe6b2ad2b9a083a6371
    permissions:
      contents: write
    secrets: inherit
