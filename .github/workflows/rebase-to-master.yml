name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@983fb2adea0987861f9ec402e6e7a13638119f2c
    permissions:
      contents: write
    secrets: inherit
