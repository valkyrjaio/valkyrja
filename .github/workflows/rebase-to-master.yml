name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@e3e801bdc0d696988d6fc331b6cf0d806b653abd
    permissions:
      contents: write
    secrets: inherit
