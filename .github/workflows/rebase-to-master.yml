name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@3812be12684beea1fbb95888e437a220f0813342
    permissions:
      contents: write
    secrets: inherit
