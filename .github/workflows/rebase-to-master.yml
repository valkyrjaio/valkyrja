name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@e650f09198bb543d16ed72e44ac1d06028a1c7cf
    permissions:
      contents: write
    secrets: inherit
