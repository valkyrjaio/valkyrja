name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@2b626b4cd5090fb9d998ba35450775bfea79b21b
    permissions:
      contents: write
    secrets: inherit
