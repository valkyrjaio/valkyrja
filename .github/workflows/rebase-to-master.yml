name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@13342de3aaf7e2c581c4b2420b59ef44a7b2c3b4
    permissions:
      contents: write
    secrets: inherit
