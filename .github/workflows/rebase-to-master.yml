name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@983eb086a57533c52b7ce86a29b2d537aad838ed
    permissions:
      contents: write
    secrets: inherit
