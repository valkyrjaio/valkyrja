name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@bcbf9696bd9a7fd71abcf4d00d3420cc949c98e1
    permissions:
      contents: write
    secrets: inherit
