name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@09dea16834afe9016704cc57fa49f351bc55af6c
    permissions:
      contents: write
    secrets: inherit
