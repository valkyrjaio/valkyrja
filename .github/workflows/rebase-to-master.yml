name: Rebase to Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} to master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-to-master.yml@fa0159e624dd543f81204bfe9419cd46e2239087
    permissions:
      contents: write
    secrets: inherit
